var notice = $('<%= j render "shared/sign_in_notice" %>').hide()

if (!$('.alert').length) {
  $('.jumbotron').prepend(notice);
  $('.alert').slideDown()

  window.setTimeout(function(){
    hideAlert();
  }, 4000);
}
