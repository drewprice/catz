<% if user_signed_in? && current_user.cats_include?(@cat) %>
  <%= link_to cat_path(@cat.giphy_id), method: :delete do %>
    DISLIKE
  <% end %>
<% else %>
  <%= link_to cat_path(@cat.giphy_id), method: :post do %>
    LIKE
  <% end %>
<% end %>
